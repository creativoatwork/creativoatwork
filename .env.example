# Cloudflare Worker URL for the contact form.
# After deploying the worker (see worker/), copy this file to .env.production
# and replace the placeholder with the real workers.dev URL.
VITE_CONTACT_URL=https://creativoatwork-contact.<your-account>.workers.dev/contact
