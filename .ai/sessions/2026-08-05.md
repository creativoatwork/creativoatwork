# 2026-08-05

## Objective

Bootstrap `.ai/` structured memory and a human-facing `README.md`. First session in this repository; no prior memory existed.

## Starting state

`main`, clean, in sync with `origin/main` at `879f567`. No README, no `.ai/`, no CI, no tests.

## What was done

Read the repository to ground the documentation in actual code rather than assumptions: `package.json`, `firebase.json`, `.firebaserc`, `.env.example`, `.gitignore`, `index.html`, `vite.config.ts`, `src/App.tsx`, `src/main.tsx`, `src/index.css`, `src/components/Contact.tsx`, `worker/src/index.ts`, `worker/wrangler.toml`, `PRODUCT.md`, plus a grep pass over the remaining components for section ids and numbering conventions.

Created:

- `README.md` — purpose, stack, prerequisites, local dev, build, both deploy paths, contact-form flow, project structure, design system, accessibility, known gaps.
- `.ai/CLAUDE.md` — project rules: scope limits, design constraints from `PRODUCT.md`, accessibility floor, contact-endpoint security rules, verification honesty, git rules.
- `.ai/CONTEXT.md` — architecture, stack, conventions, contact form contract, secrets, validation, deployment.
- `.ai/decisions.md` — bootstrap decision plus three reconstructed entries (Worker+Resend, Firebase Hosting as static host, Tailwind 4 CSS-first), explicitly marked as inferred.
- `.ai/backlog.md` — gaps across security, validation, deployment, documentation.
- `.ai/sessions/2026-08-05.md` — this log.

## Findings worth carrying forward

- Firebase is a **static host only** here — no Auth, Firestore, Storage, or Functions, and no Firebase SDK in the bundle. Easy to wrongly assume otherwise given the Firebase config files.
- The contact endpoint is the only attack surface and has no rate limiting; the honeypot is the sole abuse control.
- `VITE_CONTACT_URL` is inlined at build time, so a Worker URL change requires a frontend rebuild — not just a config change.
- `npm run build` (via `tsc -b`) is the only automated gate in the repository.
- `cleanUrls` in `firebase.json` is load-bearing for the `/terms` link.

## Verification

Documentation only — no code changed. Every factual claim was read out of the files listed above. Nothing was built or executed, and no runtime behavior was confirmed this session.

## Not done

Nothing committed — no commit was requested. `PRODUCT.md`'s empty "Register" section was left as-is pending confirmation.

---

# Session 2 — Contact endpoint rate limiting

## Objective

Close the backlog's top item: no rate limiting on the public contact endpoint.

## Design path

Brainstormed to a layered design (3/min burst + 10/hour KV cap + invisible Turnstile), then the operator constrained scope to "free, no added headaches, no complexity at all." Rescoped to the native Cloudflare rate-limiting binding alone. Full reasoning and accepted trade-offs are in `decisions.md`.

Worth noting for future work: DNS for `creativoatwork.com` is on Porkbun with an A record to Firebase Hosting (`199.36.158.100`). The domain is **not** proxied through Cloudflare, so zone-level WAF rate limiting rules are unavailable — enforcement has to live in the Worker. The Cache API is also a dead end here: it is a no-op on `workers.dev` subdomains.

## Changes

- `worker/wrangler.toml` — `CONTACT_RATE_LIMITER` ratelimit binding, 3 per 60s.
- `worker/src/index.ts` — `RateLimiter` type, `RATE_LIMIT_PERIOD_SECONDS`, and a fail-open limit check placed after honeypot and validation.
- `src/components/Contact.tsx` — `rate_limited` copy in `ERROR_COPY`.

## Verification — actually run

- `worker: npm run typecheck` → clean.
- `npm run build` (frontend) → clean, built in 407ms.
- `wrangler dev` + curl against `localhost:8787`:
  - 5 rapid posts from one IP → **502, 502, 502, 429, 429**. The 502s are expected: no `RESEND_API_KEY` locally, so Resend 401s and the worker maps it to `send_failed`. Three got through, the fourth was limited.
  - A different `CF-Connecting-IP` → 502, i.e. not limited. Per-IP keying confirmed.
  - 429 response carries `Retry-After: 60`, the correct CORS origin, and `{"ok":false,"error":"rate_limited"}`.
- The limiter bound as `[connected to remote resource]` in local dev, so this exercised real enforcement rather than a stub.

**Not verified:** behavior on the deployed worker (not deployed this session), and the browser-side rendering of the `rate_limited` message.

## Notes

- `worker/node_modules` was absent and was installed; wrangler resolved to 3.114.17. It warns that v4 is available — not upgraded, since nothing required it.
- `npm audit` reports issues in worker dev dependencies. Not investigated.

## Next

Deploy is pending and was not run — `cd worker && npm run deploy` for the limiter, plus a frontend rebuild and `firebase deploy --only hosting` for the error copy. Copy elevation for the Hero capability strip and section `01 / Services` was raised and is in progress.
